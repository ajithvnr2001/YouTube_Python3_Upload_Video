# single subtitle
# Add single caption to existing video
!python add_captions.py \
    --video-id="RswvAojntLc" \
    --language="en" \
    --name="English" \
    --file="/content/subtitles/english.srt" \
    --noauth_local_webserver

# multiple subtitle:
import subprocess

video_id = "RswvAojntLc"  # Your existing video ID

captions = [
    ("en", "English", "/content/subs/english.srt"),
    ("es", "Spanish", "/content/subs/spanish.srt"),
    ("hi", "Hindi", "/content/subs/hindi.srt")
]

for lang, name, filepath in captions:
    cmd = [
        "python", "add_captions.py",
        f"--video-id={video_id}",
        f"--language={lang}",
        f"--name={name}",
        f"--file={filepath}",
        "--noauth_local_webserver"
    ]
    result = subprocess.run(cmd, check=True)
    print(f"✓ {name} uploaded\n")
